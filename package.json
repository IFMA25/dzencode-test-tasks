{
  "name": "dzencode-test-task",
  "version": "1.0.0",
  "private": true,
  "description": "Test task for DzenCode",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "format": "prettier --ignore-path .prettierignore --ignore-path .prettierignore.local --write .",
    "format:check": "prettier --ignore-path .prettierignore --ignore-path .prettierignore.local --check .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.4.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.0.0"
}
